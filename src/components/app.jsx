import React from 'react';
import Header from './header'; // Fix the casing of the file name
import Footer from './footer'; // Fix the casing of the file name
import Note from './note'; // Fix the casing of the file name

function App() {
    return (
        <div>
            <Header />
            <Note />
            <Footer />
        </div>
    );
}

export default App;
